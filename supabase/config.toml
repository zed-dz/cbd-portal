[api]
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
port = 54323
api_url = "http://localhost"

[inbucket]
port = 54324
smtp_port = 54325
pop3_port = 54326

[storage]
enabled = true

[auth]
site_url = "https://cbd-portal-zeta.vercel.app"
additional_redirect_urls = ["https://tsizneslellcqusjwtub.supabase.co/functions/v1/xero-callback"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10

[functions.xero-callback]
verify_jwt = false   # Xero hits this directly — no Supabase JWT

[functions.xero-push]
verify_jwt = false   # We manually verify in the function body
